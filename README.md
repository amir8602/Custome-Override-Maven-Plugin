# Custome-Override-Maven-Plugin

After clone 

first
run  clean compile package install

second
Use command   "mvn co.amir:counter:1.0-SNAPSHOT:dependency-counter"
Or
Use command   "mvn counter:dependency-counter"
